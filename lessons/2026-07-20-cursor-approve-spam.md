# Cursor per-tool Approve spam

**Problem:** Routing every Cursor tool/shell through RYU Approve/Deny flooded the dock (Write/Read/shell noise). Unusable.

**Learned:** Cursor IDE hooks → **status rings only**. Dock Approve/Deny reserved for Claude Resume (+ optional ACP spike).

**Rule:** Never re-enable “approve every Cursor tool” as the product path. Selective gate or ACP only.
