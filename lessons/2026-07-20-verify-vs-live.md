# Script green ≠ live Resume

**Problem:** `verify:claude-resume` / status verifies pass while live WSL Claude still needs human sign-off.

**Learned:** Automations prove bridge+hook contracts. Live path needs Claude restart + real Write permission on the user machine.

**Rule:** Mark live checklist items `home-only`. Do not claim Phase 1 product-done from verify scripts alone.
